# Diese Datei wird ERZEUGT: env-beispiel
# Sie listet jede Variable, die der Code wirklich liest, ohne Werte.
#
# ⚠️ Sie kennt nur, was der CODE liest. Variablen, die nur der BAU braucht
# (z.B. NPM_TOKEN fuer private Registries), stehen hier NICHT und duerfen
# trotzdem nirgends geloescht werden.
#
# Zuletzt erzeugt aus 32 Quelldateien.

# ── Oeffentlich, landet im Browser-Bundle ─────────────────────
# gelesen in src/lib/site-url.ts
NEXT_PUBLIC_SITE_URL=
